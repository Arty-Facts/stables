# Test and development dependencies. Kept out of the runtime image.

-r requirements.txt

pytest>=7.0.0
pytest-cov>=4.0.0

# Tests: fastapi's TestClient needs httpx.
httpx>=0.27

# Used by the manual test scripts only (test_connection.py, test_system.py).
requests>=2.25.0
pyperclip>=1.8.0
